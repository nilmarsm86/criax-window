/**
 * Este es el paquete se trabajan las conecciones a las diferentes bd
 * 
 * DRIVER MANAGER
 * <br />
 * <code>var conn = {name:'persona', path:'persistence',driver:'sqlite'};</code>
 * <br />
 * <code>var dm = new qx.cormx.dbal.DriverManager(conn);</code>
 * <br />
 * <code>var result = dm.getConnection();</code>
 * 
 * <br /> 
 * DRIVER PERSONAL
 * <br />
 * <code>var conn = {name:'persona', path:'/home/bomby/CriaxProyects/persistencia/',driver:'sqlite'};</code>
 * <br />
 * <code>var dm = new cormx.dbal.DriverManager(conn);</code>
 * <br />
 * <code>var result = dm.getConnection(cormx.dbal.driver.Sqlite);</code>
 * <br />
 */
