/**
 * Este es el paquete se encuentran las clases para la optencion de los datos 
 * de la bd
 * 
 * FIND ALL
 * <br />
 * <code>var finder = new persistencia.entity.finder.PersonaFinder(mapper);</code>
 * <br />
 * <code>var result = finder.findAll();</code>
 * <br />          
 * FIND BY
 * <br />
 * <code>var finder = new persistencia.entity.finder.PersonaFinder(mapper);</code>
 * <br />
 * <code>var result = finder.findBy([{column:'edad',value:25}]);</code>
 * <br />          
 * FIND BY ID
 * <br />
 * <code>var finder = new persistencia.entity.finder.PersonaFinder(mapper);</code>
 * <br />
 * <code>var result = finder.findById(3);</code>
 * <br />
 * PERSONAL FIND METHOD
 * <br />
 * <code>var finder = new persistencia.entity.finder.PersonaFinder(mapper);</code>
 * <br />
 * <code>var result = finder.consultaPersonalizada();</code>
 * <br />
 * FIND ONE BY
 * <br />
 * <code>var finder = new persistencia.entity.finder.PersonaFinder(mapper);</code>
 * <br />
 * <code>var result = finder.findOneBy([{column:'edad',value:25}]);</code>
 */
